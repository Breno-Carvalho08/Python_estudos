# Exercício - Unir listas
# Crie uma função zipper (como o zipper de roupas)
# O trabalho dessa função será unir duas
# listas na ordem.
# Use todos os valores da menor lista.
# Ex.:
# ['Salvador', 'Ubatuba', 'Belo Horizonte']
# ['BA', 'SP', 'MG', 'RJ']
# Resultado
# [('Salvador', 'BA'), ('Ubatuba', 'SP'), ('Belo Horizonte', 'MG')]

from itertools import zip_longest
cidades = ['Salvador', 'Ubatuba', 'Belo Horizonte']
estados = ['BA', 'SP', 'MG', 'RJ']

# Usando zip para unir as listas - Zip pega o tamanho da menor lista e utiliza apenas os elementos até o tamanho dela
def zipper(lista1, lista2):
    return list(zip(lista1, lista2))

def zipper_longest(lista1, lista2):
    return list(zip_longest(lista1, lista2, fillvalue = 'Estado N/A'))

cidade_estado = zipper(cidades, estados)
cidade_estado_longest = zipper_longest(cidades, estados)

#Exibindo os resultados
print(cidade_estado_longest)
print(cidade_estado)

